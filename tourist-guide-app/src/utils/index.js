// Export functions from locationUtils.js
import { getCurrentLocation, geocodeLocation } from './locationUtils';

// Bundle them together to export
export { getCurrentLocation, geocodeLocation };
